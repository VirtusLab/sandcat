{
  "network": [
    {
      "action": "allow",
      "host": "peer-proxy.netbird.selfhosted",
      "port": 8080,
      "comment": "Layer 1 NetBird DNS mode: stable FQDN survives proxy-peer recreate. For IP-only fallback use the mesh IP instead."
    }
  ]
}
